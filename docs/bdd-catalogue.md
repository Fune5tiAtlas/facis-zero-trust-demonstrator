<!-- Code generated by cmd/bddpack from features/annex/annex-a.json. DO NOT EDIT. -->

# BDD catalogue

Every Annex A row, its acceptance criterion and its scenario, worded exactly as in `FACIS_ZTD_Annex_A_Final_Acceptance_Criteria_and_BDD_Test_Cases_v1.7.xlsx` (SHA-256 `c6ec2bb810ad399327d9ec208cb9258a9e0e832cc9ec1732cc9d8b9611a15860`).

| Row | Test ID | Requirement | Acceptance criterion | Scenario | Evidence | Gate | Test type | Status | Runner | File |
|---|---|---|---|---|---|---|---|---|---|---|
| ZT-01 | BDD-ZT-001 | Secure Service Mesh | All demonstrator service-to-service communication is protected by the configured service mesh and untrusted paths are blocked. | GIVEN the service mesh is deployed in each target cluster with workload identities issued WHEN two in-mesh services communicate and a workload outside the mesh attempts the same call THEN the in-mesh call is carried over the mesh under mutual TLS AND the out-of-mesh call is blocked and logged. | `evidence/bdd-zt-001/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-02 | BDD-ZT-002 | Secure Execution Environment | Critical components run only in the protected Kubernetes execution environment with signed-image/admission controls active. | GIVEN the target Kubernetes environment is ready and the approved signed release artefacts are available WHEN the Zero Trust execution environment is deployed THEN only artefacts satisfying the configured admission and trust controls reach Running state AND an unsigned or non-compliant artefact is rejected before execution. | `evidence/bdd-zt-002/` | G2 - Platform and documentation baseline (with BDD submission) | BDD / integration | pending | godog | `features/go/admission.feature` |
| ZT-03 | BDD-ZT-003 | Trust Zone | The two federation/trust zones are represented as separate participants with isolated cluster contexts and explicit trust relationships. | GIVEN the two federation zones are deployed as separate participants in isolated cluster contexts WHEN the configured trust relationship between them is exercised THEN each zone resolves the other as an explicitly configured participant AND a zone outside the configured relationship is not resolvable as a participant. | `evidence/bdd-zt-003/` | G2 - Platform and documentation baseline (with BDD submission) | BDD / integration | pending | godog | `features/go/trust-lists.feature` |
| ZT-04 | BDD-ZT-004 | Secure Connection | ZT Connector A and ZT Connector B establish a protected authenticated channel before application traffic is permitted. | GIVEN two configured Zero Trust Connectors with trusted identities WHEN Connector A initiates a connection to Connector B THEN an authenticated protected channel is established before application traffic is forwarded AND a peer that cannot establish the required trusted channel receives no application traffic. | `evidence/bdd-zt-004/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-05 | BDD-ZT-005 | Communication Visualization | The demonstrator visibly presents connection/trust state, decisions and communication outcomes to an external audience. | GIVEN the client-approved visualization concept and a running FACIS.ZTD demonstrator WHEN the communication-visualization scenario is exercised through the ORCE-orchestrated user journey THEN the demonstrator visibly presents connection and trust state, the decisions taken and the resulting communication outcomes to an external audience AND a blocked communication is presented with its refusal reason. | `evidence/bdd-zt-005/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-06 | BDD-ZT-006 | Attribute/Credential Based Access Control | Participant-backend access to protected resources is decided from verified attributes/credentials and corresponding policy rules. Access control is applied as attribute-based access control (ABAC) on the inner channel from participant backend to protected resource. | GIVEN one participant backend with the required credential attributes and one without them WHEN both request the same protected resource THEN the authorized request is permitted according to credential/policy evaluation AND the request without matching attributes/authorization is denied and evidenced. | `evidence/bdd-zt-006/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/policy.feature` |
| ZT-07 | BDD-ZT-007 | API Dock | A participant-backend API and protected-resource API can be integrated/replaced through documented interfaces without changing the Zero Trust core. | GIVEN the documented participant-backend and protected-resource interfaces WHEN a replacement API is docked in place of the supplied mock THEN it serves the demonstrator flow with no change to Connector, policy or trust configuration AND an API that does not satisfy the documented interface is rejected at configuration time. | `evidence/bdd-zt-007/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-08 | BDD-ZT-008 | Visualization | The UI presents the key Zero Trust functionality and full demonstrator flow in a comprehensible client-approved visualization. | GIVEN the client-approved visualization concept and a running demonstrator WHEN an external observer follows the demonstrator flow in the UI THEN the key Zero Trust functionality and the complete flow are presented comprehensibly in the approved form AND a step that produces no visible outcome is recorded as a defect. | `evidence/bdd-zt-008/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-09 | BDD-ZT-009 | User Journey | The client-aligned scenario is orchestrated step by step by ORCE and demonstrates the complete end-to-end Zero Trust flow. | GIVEN the client-aligned user-journey scenario configured in ORCE WHEN the journey is run from participant registration through to the protected-resource response THEN ORCE orchestrates each step in sequence and the complete end-to-end Zero Trust flow is demonstrated AND a failing step halts the journey with its reason shown. | `evidence/bdd-zt-009/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-10 | BDD-ZT-010 | Image Signing and Attestation | Custom GitHub runner up and running, Key material can be uploaded on the runner, Runner setup AMD64-based systems. Signing and attestation flows can be triggered by authorised maintainers and committers only. | GIVEN the approved GitHub runner, signing key material and a release image WHEN the image-signing and mock-attestation workflow runs THEN the image is signed and the attestation artefact is produced by the authorized pipeline AND a release without the required signature cannot pass the admission path. | `evidence/bdd-zt-010/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-11 | BDD-ZT-011 | OPA Gatekeeper | All Docker images signed by the client, Gatekeeper blocks unsigned images during pod creation, Trust Anchor is provided via did:web, Other Governance Rego Policies can be executed over Gatekeeper, e.g., allowed registries, naming conventions or similar. Trust anchor key material is stored in OpenBao, or used via the transit engine and TSA crypto provider service, and is linked over TRAIN. | GIVEN OPA Gatekeeper deployed with the client trust anchor published via did:web over TSA and linked in TRAIN, and the signing key material held in OpenBao WHEN pods are created from a correctly signed image and from an unsigned image THEN the signed image is admitted and the unsigned image is blocked at pod creation AND additional governance Rego policies, such as allowed registries and naming conventions, are enforced through Gatekeeper. | `evidence/bdd-zt-011/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/admission.feature` |
| ZT-12 | BDD-ZT-012 | OPA Policy Repository | Policy artifacts can be built and uploaded to Harbor; Gatekeeper loads and uses the policies from Harbor. Policy packages are built from a repository path to which only authorised maintainers and committers have write access. | GIVEN an approved Rego policy package in the FACIS source repository WHEN the GitHub Actions policy workflow runs THEN the policy artefact is built and pushed to Harbor via OCI and Gatekeeper loads the published policy AND an unavailable or invalid policy package is not treated as an approved admission policy. | `evidence/bdd-zt-012/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/admission.feature` |
| ZT-13 | BDD-ZT-013 | Linux-based Docker Images | All Docker images are Linux-based, All Docker images up and running. | GIVEN the release image set for the demonstrator WHEN the images are inspected and deployed to the target cluster THEN every image is Linux-based and reaches Running state AND an image that is not Linux-based is rejected before deployment. | `evidence/bdd-zt-013/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-14 | BDD-ZT-014 | T-Systems Open Sovereign Cloud Environment | Cluster and the solution are reachable. All ZT components deployed and in Running state, OPA Gatekeeper demonstrably blocking unsigned images, End-to-end connection between ZT Connector and protected resource verified. | GIVEN the T-Systems Open Sovereign Cloud environment provisioned for the demonstrator WHEN the full Zero Trust stack is deployed to it THEN the cluster and solution are reachable, all Zero Trust components reach Running state, OPA Gatekeeper demonstrably blocks an unsigned image, and an end-to-end connection between the ZT Connector and the protected resource is verified. | `evidence/bdd-zt-014/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/environments.feature` |
| ZT-15 | BDD-ZT-015 | IONOS Cloud Environment | Visualization of the Demonstrator runs on the IONOS Cloud. | GIVEN the IONOS Kubernetes environment and the approved visualization release WHEN the visualization stack is deployed THEN the FACIS Zero Trust Demonstrator visualization is reachable and operational on IONOS Cloud. | `evidence/bdd-zt-015/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/environments.feature` |
| ZT-16 | BDD-ZT-016 | Cloud Environments | Documentation can be demonstrated for reproducing the setup of each test cluster step- by-step. | GIVEN the accepted release baseline WHEN cloud environments documentation is generated from the repository THEN the documented setup/usage can be reproduced step by step and the documentation is buildable from GitHub. | `evidence/bdd-zt-016/` | G2 - Platform and documentation baseline (with BDD submission) | Documentation / client review | pending | Cucumber.js | `features/js/documentation.feature` |
| ZT-17 | BDD-ZT-017 | Demonstrator Usage | Documentation can be demonstrated for reproducing the setup of each test cluster step-by-step. The Demonstrator manual is generated automatically by a GitHub Actions workflow and published. | GIVEN the accepted release baseline WHEN demonstrator usage documentation is generated from the repository THEN the documented setup/usage can be reproduced step by step and the documentation is buildable from GitHub. | `evidence/bdd-zt-017/` | G2 - Platform and documentation baseline (with BDD submission) | Documentation / client review | pending | Cucumber.js | `features/js/documentation.feature` |
| ZT-18 | BDD-ZT-018 | TRAIN Allow List | Connector reads TRAIN trust lists to allow counter-connector connections. | GIVEN a TRAIN trust list naming the permitted counter-connector WHEN connections are attempted from a listed and from an unlisted counter-connector THEN the listed peer is admitted on the basis of the retrieved trust list AND the unlisted peer is refused. | `evidence/bdd-zt-018/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/trust-lists.feature` |
| ZT-19 | BDD-ZT-019 | Upstream / Downstream Proxy Functionality | Connector uses and establishes the TLS channel. | GIVEN two Zero Trust Connectors with an established attested TLS channel WHEN an upstream request including relevant HTTP/gRPC metadata is forwarded through the connector pair THEN the request reaches the counter side over that protected channel with required call information preserved AND no unprotected inter-connector path is used. | `evidence/bdd-zt-019/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-20 | BDD-ZT-020 | Guarding Functionality | Connector can guard a protected resource, No access without matching policies and authorizations. Enforcement is based on Rego policies evaluated through the TSA Policy Engine; the guard is deployable per protected resource over both REST and gRPC; unauthorised access is answered with an OID4VP link. | GIVEN a protected resource behind the Connector guard WHEN a request with matching authorization and a request without matching authorization are executed THEN the authorized request passes the PEP/PDP policy chain AND the unauthorized request is denied before the protected resource returns data. | `evidence/bdd-zt-020/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-21 | BDD-ZT-021 | OAuth2 Server | Server issues access tokens based on OID4VP presentations, Server issues access tokens to participant backends for accessing the connector itself. New participant backends are registered through Dynamic Client Registration. | GIVEN a registered participant backend and a valid OID4VP presentation WHEN the Connector OAuth2 server processes the respective flows THEN it issues the appropriate DPoP-bound access tokens AND an invalid presentation or unregistered/unauthorized client does not receive a usable access token. | `evidence/bdd-zt-021/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-22 | BDD-ZT-022 | Token Storage | Participant backend can call the protected resource over the connector, Upstream authorization is replaced via the token storage on DPOP basis. The token store performs token renewal and the associated OID4VP tasks without manual intervention. | GIVEN a participant-backend token and a stored counter-connector resource token WHEN the backend calls the protected resource through the Connector THEN the backend token and DPoP proof are validated and the correct counter-side token is used upstream AND an invalid DPoP proof or expired/unrenewable token causes the request to be denied. | `evidence/bdd-zt-022/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-23 | BDD-ZT-023 | Service Mesh Implementation | A service mesh based on Istio or Cilium is implemented in each Kubernetes cluster, Communication between services is routed through the service mesh. | GIVEN each target Kubernetes cluster with the selected Istio- or Cilium-based service mesh installed WHEN services communicate within a cluster THEN the mesh is present in every cluster and service traffic is routed through it AND traffic attempting to bypass the mesh data path is not delivered. | `evidence/bdd-zt-023/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-24 | BDD-ZT-024 | SPIFFE/SPIRE Deployment | SPIRE is deployed in each Kubernetes cluster, The service mesh is configured to use SPIRE-provided identities, Appropriate SPIRE node- and workload attestation is used, All services participating in the service mesh are registered as SPIRE workloads. | GIVEN each target Kubernetes cluster is running WHEN SPIRE node and workload registration is applied THEN SPIRE is deployed in every cluster, all mesh services obtain SPIRE-backed identities and the service mesh uses those identities AND a workload that cannot attest does not receive an identity. | `evidence/bdd-zt-024/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/workload-identity.feature` |
| ZT-25 | BDD-ZT-025 | Routing Policies | The service mesh is configured to enforce network policies, Network policies are deployed to allow related services to communicate, A default deny policy is in place to prevent unaffiliated service communication. | GIVEN related and unrelated workloads in the service mesh WHEN east-west communication is attempted THEN policy-related workloads communicate according to configured trust policy AND unaffiliated workloads are blocked by the default-deny network policy without relying on IP-address identity. | `evidence/bdd-zt-025/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-26 | BDD-ZT-026 | Ingress and Egress | Ingress and egress for all inter-service communication is configured to use the ZT connector, Other APIs use a different appropriate ingress and egress configuration. | GIVEN application traffic, DNS traffic and an observability/admin endpoint WHEN each traffic type leaves or enters a cluster THEN protected inter-cluster application traffic is routed through the ZT Connector AND explicitly exempt infrastructure/admin traffic uses its separately controlled ingress/egress path. | `evidence/bdd-zt-026/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-27 | BDD-ZT-027 | Observability | OTEL logs available. Telemetry is exported through the OpenTelemetry Collector, metrics are retained in Prometheus and traces are available in an OTel-compatible tracing backend. | GIVEN the OpenTelemetry Collector, Prometheus and the OTel-compatible tracing backend are deployed WHEN demonstrator traffic and security decisions occur THEN traces and metrics are exported from the cluster through the Collector, metrics are retained in Prometheus and traces are queryable in the tracing backend AND a component emitting no telemetry is visible as a gap in the correlated trace. | `evidence/bdd-zt-027/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-28 | BDD-ZT-028 | Attested TLS | All inter-cluster communication uses the attested TLS protocol, An appropriate TLS version is used, TLS certificates are correctly checked against the cluster’s identities. | GIVEN two ZT Connectors in different clusters WHEN they establish inter-cluster communication THEN the channel uses mutually attested TLS with TLS 1.3 or later and certificates are checked against cluster identity AND a TLS version below 1.3 or failed identity/attestation validation prevents channel establishment. | `evidence/bdd-zt-028/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-29 | BDD-ZT-029 | Attestation Request | The attestation request message is sent in a machine-readable format, The information contained in the message includes the desired attestation type, including at least: • Server-Side Attestation: Only the server provides an attestation report, • Client-Side Attestation: Only the client provides an attestation report, • Mutual Attestation: Both server and client provide an attestation report. The size of any additional information contained in the message does not exceed the size of the attestation report. | GIVEN a Connector initiates an attested-TLS handshake WHEN it creates an attestation request THEN the request is machine-readable and states the requested server-side, client-side or mutual attestation mode AND malformed requests or unsupported required attestation information do not advance the handshake. | `evidence/bdd-zt-029/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-30 | BDD-ZT-030 | Attestation Response | The attestation response message is sent in a machine-readable format, The attestation response contains an attestation report, The size of any additional information contained in the message does not exceed the size of the attestation report. | GIVEN a valid machine-readable attestation request WHEN the peer creates its response THEN the response contains the required attestation report (or an empty/null report where none was requested) and respects the specified size constraint AND malformed responses are rejected by the receiving protocol flow. | `evidence/bdd-zt-030/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-31 | BDD-ZT-031 | Attestation Report Format | All attestation report signatures can be verified using common cryptography libraries (e.g. OpenSSL), All attestation reports can be verified by the receiver without special knowledge; if additional information besides the attestation report is needed (e.g. an intermediary certificate), it is provided in TRAIN or as part of the attestation response message, All attestation reports contain the TLS-exporter channel binding for the TLS channel through which they are sent. | GIVEN an attestation report received during the TLS handshake WHEN the peer verifies it THEN the report signature is cryptographically verifiable, required supporting information is available, and the TLS-exporter channel binding matches the active TLS channel AND a report with invalid signature or channel binding is rejected. | `evidence/bdd-zt-031/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-32 | BDD-ZT-032 | Verification Result Message | The verification result message is sent in a machine-readable format, An affirmative verification result message is sent only if the attestation report sent by the peer could be verified successfully, The size of any additional information contained in the message does not exceed the size of the attestation report. | GIVEN a peer has completed attestation-report verification WHEN it sends the verification-result message THEN the message is machine-readable and affirmative only after successful verification AND a failed verification never produces an affirmative result. | `evidence/bdd-zt-032/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-33 | BDD-ZT-033 | Error Message | For any error case in each peer implementation, an error message is sent, The error message is machine-readable, The size of any additional information contained in the message does not exceed the size of the attestation report, Error messages are correctly received and lead to connection termination. | GIVEN an error occurs at any defined step of the attestation protocol WHEN the peer handles the error THEN it emits a machine-readable error containing a useful human-readable diagnostic and closes the TLS connection AND no application-layer communication continues after the error. | `evidence/bdd-zt-033/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-34 | BDD-ZT-034 | Proxy Integration | The attested-TLS reverse proxy is configured from Kubernetes Ingress or Gateway API resources, integrates with the service mesh for inbound routing to member services, and is implemented in Go. | GIVEN an Ingress or Gateway API resource declaring an attested-TLS-protected route WHEN the resource is applied and a peer connector initiates an attested connection THEN the reverse proxy accepts the attested upstream connection and routes it to the configured in-mesh backend AND a route whose peer cannot be attested receives no application traffic. | `evidence/bdd-zt-034/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-35 | BDD-ZT-035 | Hash Management/TRAIN Integration | Upon connection establishment, the attested TLS endpoint fetches the peer’s hash from TRAIN, Connection establishment proceeds only if the hash matches the value in the attestation report. Each endpoint's CI/CD pipeline pre-generates its own launch digest and uploads it to TRAIN on every deployment. | GIVEN each attested-TLS endpoint has published its expected launch digest/measurement to TRAIN WHEN a peer connection is established THEN the remote expected hash is resolved from TRAIN and compared with the received attestation report AND a missing or mismatching trusted hash prevents connection establishment. | `evidence/bdd-zt-035/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/trust-lists.feature` |
| ZT-36 | BDD-ZT-036 | OPA Gatekeeper | Gatekeeper consumes the policies and enforces them. Gatekeeper loads Rego policy packages in combination with the XFSC TSA Policy Service. | GIVEN OPA Gatekeeper deployed together with the XFSC TSA Policy Service and a published Rego policy package WHEN a workload subject to that policy is submitted for admission THEN Gatekeeper loads the policy through the TSA Policy Service and enforces the decision AND a workload violating the policy is denied at admission. | `evidence/bdd-zt-036/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/admission.feature` |
| ZT-37 | BDD-ZT-037 | OPA Gatekeeper Policies | Policies are available; a GitHub push action is in place. Policies are distributed as tarball packages through XFSC Harbor over the OCI interface. | GIVEN an approved Gatekeeper/Rego policy source in the FACIS repository WHEN the policy publication workflow runs THEN the policy package is built and published to Harbor and the required GitHub push action completes successfully. | `evidence/bdd-zt-037/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/admission.feature` |
| ZT-38 | BDD-ZT-038 | Container Signing Pipeline | OPA Gatekeeper accepts Docker images which are signed by the client. The public verification keys are published so that signatures can be verified independently. | GIVEN a release image and the client-controlled signing setup WHEN the GitHub Actions signing workflow completes THEN the image signature is published and Gatekeeper accepts the correctly signed image AND an unsigned or incorrectly signed image is rejected at admission. | `evidence/bdd-zt-038/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-39 | BDD-ZT-039 | Participant Backend Mockup | Participant backend can register itself, Participant backend can obtain access tokens, Participant backend can successfully pass PEP proxy to connect Zero Trust connector and its channel, Participant backend gets an answer from protected resource mock, Visualization reflects this interaction. | GIVEN the participant-backend mock is configured against the ZT Connector WHEN it dynamically registers and requests protected-resource access THEN it obtains the required access token, passes the Connector/PEP path, receives the protected-resource response and exposes the interaction to the visualization AND a failed registration/authorization is surfaced as a refusal outcome. | `evidence/bdd-zt-039/` | G5 - Code ready for QA + ORCE user journey and visualization | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-40 | BDD-ZT-040 | Credential-Based Access Control (CrBAC) | A participant backend can use the Zero Trust Channel of the Connector to call protected resource, and the Connector will add the obtained Access Tokens of the counter connector to each call, DPOP Verification of each participant backend call and matching it against internal JWT storage, OCM W-Stack Presentations can be triggered by using OID4VP Verifier requests from counter connector. | GIVEN a participant backend with a valid credential presentation and DPoP-capable client identity WHEN it accesses the counter-side protected resource through the ZT Connector THEN the Connector obtains/maintains the counter-side access token via OCM/OID4VP, validates DPoP, substitutes the correct upstream authorization and returns the protected response AND revoked/invalid credentials, invalid DPoP or unusable tokens result in denial. | `evidence/bdd-zt-040/` | G5 - Code ready for QA + ORCE user journey and visualization | BDD / integration | pending | godog | `features/go/credential-lifecycle.feature` |
| ZT-41 | BDD-ZT-041 | Protected Resource Mockup | Protected resource responds different results based on roles. | GIVEN the protected-resource mock configured with role-differentiated responses WHEN it is called with access tokens carrying different roles THEN each role receives its configured response AND a token carrying no permitted role receives no protected content. | `evidence/bdd-zt-041/` | G5 - Code ready for QA + ORCE user journey and visualization | BDD / integration | pending | godog | `features/go/policy.feature` |
| ZT-42 | BDD-ZT-042 | UX Concept | GitHub documentation and GitHub Page ready, Concept aligned with the client. | GIVEN the initial demonstrator flow WHEN the wireframe/visualization concept is reviewed with FACIS THEN the concept is documented in GitHub and the client approval record is captured before UI implementation proceeds. | `evidence/shared/bdd-zt-042-079/` | G2 - Platform and documentation baseline (with BDD submission) | Documentation / client review | pending | Cucumber.js | `features/js/documentation.feature` |
| ZT-43 | BDD-ZT-043 | ORCE Usage | ORCE is used. | GIVEN the demonstrator deployed with ORCE as the orchestration runtime WHEN the visualization user journey is executed THEN every step of the journey is driven by an ORCE flow that is identifiable in the ORCE runtime AND no journey step bypasses ORCE. | `evidence/bdd-zt-043/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-44 | BDD-ZT-044 | Value Visualization | Concept for value visualization aligned with the client, Values presented in Demonstrator. The presented values include TEE measurements, credentials, TRAIN zones, communication decisions and messages. | GIVEN the client-aligned value-visualization concept and a running demonstrator WHEN the user journey produces trust and communication outcomes THEN the demonstrator presents the TEE measurements, credentials, TRAIN zones, communication decisions and messages defined in the concept AND a value that cannot be obtained is shown as unavailable rather than omitted. | `evidence/bdd-zt-044/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-45 | BDD-ZT-045 | Demonstrator Configuration | Configuration documented, Configuration within the Demonstrator for certain parts can be demonstrated. The configuration interface covers TRAIN zones and policies. | GIVEN the documented demonstrator configuration interface WHEN an operator changes the TRAIN zone and policy configuration through it THEN the change is applied and reflected in the running demonstrator AND an invalid configuration is rejected with a machine-readable error. | `evidence/bdd-zt-045/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-46 | BDD-ZT-046 | User Journey Scenario | Scenario is aligned and documented on GitHub, Scenario is integrated into the Demonstrator and orchestrated by ORCE step-by- step, Scenario is illustrated with animations and images. The issuing process provides the credential to OCM W-Stack. | GIVEN the client-approved user journey WHEN it is executed in ORCE THEN the journey is shown step by step, including credential issuance/presentation, trust decisions, protected-resource access and visual outcomes. | `evidence/shared/bdd-zt-046-081/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-47 | BDD-ZT-047 | Continuous Authentication Enforcement | All access requests require authentication and authorization, Session re-validation occurs at configurable intervals (≤ 15 minutes), Access is revoked automatically if session becomes non-compliant. | GIVEN an authenticated session with a configurable re-validation interval of 15 minutes or less WHEN protected-resource requests continue over time THEN authentication and authorization are re-evaluated at the configured interval AND access is revoked automatically when the session becomes non-compliant. | `evidence/bdd-zt-047/` | G6 - Quality readiness | Negative-path BDD | pending | godog | `features/go/fail-secure.feature` |
| ZT-48 | BDD-ZT-048 | Least Privilege Access Control | RBAC or ABAC is implemented, Privileged access rights are time-bound and require explicit approval, | GIVEN identities with different approved roles/attributes WHEN they request functions with different privilege requirements THEN each receives only the minimum permitted access and any privileged entitlement is time-bound/explicitly approved AND requests beyond the granted privilege are denied. | `evidence/bdd-zt-048/` | G6 - Quality readiness | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-49 | BDD-ZT-049 | Micro-Segmentation | Workloads are segmented by policy-defined trust zones, East-west traffic is inspected and authorized, Unauthorized lateral communication attempts are blocked and logged. | GIVEN workloads in separate policy-defined trust zones WHEN authorized and unauthorized east-west connections are attempted THEN permitted zone-to-zone traffic is inspected and authorized AND unauthorized lateral movement is blocked and logged. | `evidence/bdd-zt-049/` | G6 - Quality readiness | Negative-path BDD | pending | godog | `features/go/plane-separation.feature` |
| ZT-50 | BDD-ZT-050 | Cryptographic Standards (BSI TR-02102) | No deprecated algorithms present in any component. Key lengths documented and auditable. Symmetric encryption uses AES-256 or equivalent, asymmetric encryption uses RSA-4096 or ECC P-256/P-384, and hash functions use SHA-256 or stronger. | GIVEN the deployed demonstrator configuration and generated cryptographic artefacts WHEN algorithms, key lengths, hashes and TLS settings are inspected THEN only BSI TR-02102-compliant algorithms/strengths are present and auditable AND deprecated algorithms such as MD5, SHA-1 or insufficient RSA key lengths are absent/rejected. | `evidence/bdd-zt-050/` | G6 - Quality readiness | Technical BDD / inspection | pending | godog | `features/go/security-baseline.feature` |
| ZT-51 | BDD-ZT-051 | Credential Revocation | Revocation check performed at every credential verification. Revocation endpoint availability is monitored. Revocation uses a standards-compliant mechanism, either W3C Bitstring Status List v1.0 or IETF Token Status List, and unavailability of the revocation infrastructure results in fail-closed behaviour. | GIVEN a credential is revoked or revocation status cannot be verified WHEN the credential is presented for protected access THEN verification fails closed and the protected resource is not reached. | `evidence/bdd-zt-051/` | G6 - Quality readiness | Negative-path BDD | pending | godog | `features/go/credential-lifecycle.feature` |
| ZT-52 | BDD-ZT-052 | Fail-Secure (Fail-Closed) Behavior | Access denied when Policy Engine is unreachable (verified in test). Control plane failure detected and alerted within 30 seconds. Degradation scenarios documented with defined fail-closed behavior. Unavailability is demonstrated for each named control-plane component: Policy Engine, TRAIN, SPIRE and OCM. | GIVEN a required Zero Trust control-plane service becomes unavailable WHEN protected access is requested THEN access is denied, the failure is detected within 30 seconds and an alert/evidence record is produced. | `evidence/bdd-zt-052/` | G6 - Quality readiness | Negative-path BDD | pending | godog | `features/go/fail-secure.feature` |
| ZT-53 | BDD-ZT-053 | Multi-Tenancy and Tenant Isolation | Tenant data, credentials and policy configuration are isolated by Kubernetes namespace; cross-tenant access attempts are denied and evidenced. | GIVEN two organizational tenants represented by separate Kubernetes namespaces WHEN each tenant accesses its own and the other tenant's data, credentials or policy configuration THEN own-tenant access follows authorization policy AND cross-tenant access is denied and evidenced. | `evidence/bdd-zt-053/` | G6 - Quality readiness | Technical BDD / inspection | pending | godog | `features/go/plane-separation.feature` |
| ZT-54 | BDD-ZT-054 | Software Bill of Materials (SBOM) | SBOM generated in CycloneDX or SPDX format for every release. SBOM published in GitHub repository alongside release artefacts. The SBOM is signed with the client's key material. | GIVEN a release build in the CI/CD pipeline WHEN the release workflow completes THEN an SBOM in CycloneDX or SPDX format is generated automatically, signed with the client key material and published in the repository alongside the release artefacts AND a release without a signed SBOM does not pass the release gate. | `evidence/bdd-zt-054/` | G6 - Quality readiness | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-55 | BDD-ZT-055 | Management Plane / Data Plane Separation | Management plane components not reachable from data plane (verified in network test). Separation enforced at both network policy and service mesh layers. Architecture diagram clearly shows plane separation. The management-plane components covered are the Policy Engine, TRAIN, the SPIRE control plane, OCM and the observability stack. | GIVEN management-plane services and application/data-plane workloads are deployed WHEN reachability is tested from both planes THEN management services are reachable only through dedicated control channels and are separated by network-policy and service-mesh controls AND data-plane compromise/reachability does not provide access to management-plane endpoints. | `evidence/bdd-zt-055/` | G6 - Quality readiness | Technical BDD / inspection | pending | godog | `features/go/plane-separation.feature` |
| ZT-56 | BDD-ZT-056 | CI/CD Pipeline Security (Zero Trust for the Pipeline) | All third-party GitHub Actions pinned to commit SHAs. Pipeline runs with minimum REQUIRED permissions (no wildcard write access). No secrets visible in any pipeline log output. Pipeline-generated artefacts are signed before use, all pipeline runs are logged, and branch protection enforces code review before merge to main. | GIVEN the project GitHub Actions workflows and branch-protection configuration WHEN a pipeline run and a merge to main are attempted THEN all third-party actions are pinned to commit SHAs, the run holds only the minimum required permissions, no secret value appears in any log, artefacts are signed before use and the run is logged AND a merge to main without the required review is refused. | `evidence/bdd-zt-056/` | G6 - Quality readiness | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-57 | BDD-ZT-057 | SPIFFE/SPIRE Setup | SPIFFE/SPIRE is set up and each microservice has access to a SPIRE Agent on the same node. | GIVEN the target Kubernetes nodes are ready WHEN SPIFFE/SPIRE is installed and workloads are registered THEN each relevant microservice has access to a SPIRE Agent on its node and can request workload identity. | `evidence/bdd-zt-057/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/workload-identity.feature` |
| ZT-58 | BDD-ZT-058 | Service Mesh Setup | The service mesh and control plane are enabled; traffic capture uses workload certificates for mutual TLS on each node. | GIVEN SPIRE-backed identities and the target clusters WHEN the service mesh/control plane and traffic-capture layer are enabled THEN service traffic is captured on each node and protected with workload-certificate-based mutual TLS. | `evidence/bdd-zt-058/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-59 | BDD-ZT-059 | Successful Identification | The microservice successfully attests itself against the SPIRE Agent and receives an identity. | GIVEN a correctly registered microservice satisfying SPIRE attestation WHEN it requests its workload identity THEN SPIRE attests the workload successfully and issues the expected identity, which is then usable by the mesh. | `evidence/bdd-zt-059/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/workload-identity.feature` |
| ZT-60 | BDD-ZT-060 | Unsuccessful Identification | A microservice that fails SPIRE attestation receives no identity and cannot connect to any other service in the mesh. | GIVEN a workload cannot satisfy SPIRE attestation WHEN it requests a workload identity and attempts mesh communication THEN no identity is issued and all service communication is denied. | `evidence/bdd-zt-060/` | G3 - Protected-service vertical slice | Negative-path BDD | pending | godog | `features/go/workload-identity.feature` |
| ZT-61 | BDD-ZT-061 | Traffic routed via Service Mesh | All communication of the microservice is routed via the service mesh. | GIVEN a microservice participating in the configured mesh WHEN it communicates with another service THEN its service communication traverses the service-mesh data path and is subject to the configured identity and policy controls. | `evidence/bdd-zt-061/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-62 | BDD-ZT-062 | Limited Communication within Mesh | The microservice is only able to communicate with identities permitted in the configuration. | GIVEN one identity explicitly permitted to communicate with a target service and one identity not permitted WHEN both attempt the connection THEN the permitted identity communicates successfully AND the non-permitted identity is blocked by mesh policy. | `evidence/bdd-zt-062/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/mesh.feature` |
| ZT-63 | BDD-ZT-063 | TEE Mock Setup | The TEE mock setup is enabled and generates demo attestations. | GIVEN the demonstrator remote-attestation environment WHEN the TEE mock is started THEN it produces the defined mock measurement/attestation material in the format required by the attested-TLS flow AND malformed/unavailable mock attestation material is not accepted as trusted evidence. | `evidence/bdd-zt-063/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-64 | BDD-ZT-064 | Expected Measurement Retrieval | A ZT Connector can fetch expected mocked TEEM values from other connectors via TRAIN. | GIVEN a counter-connector has published its expected mocked TEE measurement to TRAIN WHEN the Connector prepares to verify that peer THEN it retrieves the expected measurement from TRAIN and uses it for comparison AND a peer with no published measurement cannot be verified. | `evidence/bdd-zt-064/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/trust-lists.feature` |
| ZT-65 | BDD-ZT-065 | Connection Start | ZT Connector 1 starts a connection by sending an attestation request. | GIVEN Connector 1 is configured to establish an attested connection to Connector 2 WHEN the connection starts THEN Connector 1 sends the required machine-readable attestation request and the handshake enters the attestation flow. | `evidence/bdd-zt-065/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-66 | BDD-ZT-066 | Remote Attestation | ZT Connector 2 responds to the attestation request by creating an attestation report and sending it to ZT Connector 1. | GIVEN Connector 2 receives a valid attestation request WHEN it performs remote attestation THEN it creates the required attestation report and sends it to Connector 1 in the protocol response. | `evidence/bdd-zt-066/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-67 | BDD-ZT-067 | Attestation Report Verification | ZT Connector 1 verifies the attestation report by comparing the expected mocked TEEM against the mocked TEEM values in the TCR. | GIVEN Connector 1 has received Connector 2's attestation report and the expected mocked TEE measurement from the TCR/TRAIN path WHEN verification is executed THEN the observed and expected values plus cryptographic/channel-binding checks are validated AND any mismatch causes verification failure. | `evidence/bdd-zt-067/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-68 | BDD-ZT-068 | Verification Result | ZT Connector 1 informs ZT Connector 2 about the result of the verification. | GIVEN Connector 1 has completed verification of Connector 2's attestation report WHEN it issues the verification-result message THEN Connector 2 receives a machine-readable result reflecting the actual outcome AND a failed verification is reported as failure, never as success. | `evidence/bdd-zt-068/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-69 | BDD-ZT-069 | Established Connection | The handshake is successful and application-layer communication can take place via the established connection. | GIVEN a successful attested-TLS handshake between two Connectors WHEN application traffic is sent over the established channel THEN the traffic is carried end to end over that channel AND traffic offered before the handshake completes is not carried. | `evidence/bdd-zt-069/` | G4 - Bidirectional inter-cluster trust | BDD / integration | pending | godog | `features/go/attested-channel.feature` |
| ZT-70 | BDD-ZT-070 | Refused Connection | If handshake or attestation verification fails, no application-layer communication can take place. It is proven that no application-layer communication can take place. | GIVEN the peer attestation report or handshake is invalid WHEN the inter-cluster connection is established THEN the protocol terminates and no application-layer communication is possible. | `evidence/bdd-zt-070/` | G4 - Bidirectional inter-cluster trust | Negative-path BDD | pending | godog | `features/go/attested-channel.feature` |
| ZT-71 | BDD-ZT-071 | Image Signing and mock Attestation | A defined set of Harbor images can be signed via CI/CD, signatures are uploaded to Harbor, and a mock attestation is generated in JSON format. The mock attestation is generated in JSON format for any TEE vendor. | GIVEN a defined set of Harbor images and the approved CI/CD signing setup WHEN the secure-execution release workflow runs THEN those images are signed, signatures are uploaded to Harbor and a JSON mock-attestation artefact is generated AND an image lacking the expected signature/attestation cannot satisfy the secure-execution gate. | `evidence/bdd-zt-071/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/supply-chain.feature` |
| ZT-72 | BDD-ZT-072 | Secure Pod Startup | OPA Gatekeeper is configured with policy and the cluster starts only properly signed images. | GIVEN Gatekeeper is configured with the approved admission policy WHEN one correctly signed image and one unsigned/non-compliant image are submitted for pod creation THEN the signed compliant pod may start AND the unsigned/non-compliant pod is denied before startup. | `evidence/bdd-zt-072/` | G2 - Platform and documentation baseline (with BDD submission) | Technical BDD / inspection | pending | godog | `features/go/admission.feature` |
| ZT-73 | BDD-ZT-073 | Authorized Upstreaming over the Zero Trust Connector | The participant backend authorizes and connects to the ZT Connector, successfully calls the protected resource over the trusted channel, and receives a policy-authorized response. | GIVEN a registered participant backend with valid authorization and an available protected resource WHEN the backend calls through the Zero Trust Connector THEN the request is authorized, traverses the trusted channel and receives the policy-permitted protected-resource response AND an unauthorized request does not return protected data. | `evidence/bdd-zt-073/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-74 | BDD-ZT-074 | Participant Backend Mock | The participant backend mock simulates a participant backend using the trusted channel and pushes status data to the visualization. | GIVEN the participant-backend mock is configured with the approved Connector endpoints WHEN the demonstrator scenario runs THEN the mock behaves as a participant backend using the trusted channel and publishes its registration/request/status events to the visualization. | `evidence/bdd-zt-074/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-75 | BDD-ZT-075 | Protected Resource Mock | The protected resource mock reacts to combinations of access rights and all accesses are visualized. | GIVEN the protected-resource mock exposes responses for different access roles WHEN requests with different authorized and unauthorized roles are executed THEN role-appropriate responses are returned and visualized AND unauthorized roles do not receive protected content. | `evidence/bdd-zt-075/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/policy.feature` |
| ZT-76 | BDD-ZT-076 | Credential Based Access | Protected resources return responses based on credentials; credentials are exchanged for JWT tokens and mapped into roles. | GIVEN credentials that map to different application roles and a credential that is invalid/revoked WHEN they are exchanged/verified and used to access the protected resource THEN valid credentials are mapped into the intended JWT/role authorization and return the permitted response AND invalid/revoked credentials do not grant protected access. | `evidence/bdd-zt-076/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-77 | BDD-ZT-077 | Mock Replaceable | All mocks are replaceable by demonstrating a real-world API. | GIVEN the participant-backend and protected-resource mocks in the running demonstrator WHEN each is replaced by a real-world API through the documented interface THEN the demonstrator journey completes against the real API with no change to the Zero Trust core AND the replacement is demonstrated end to end at least once. | `evidence/bdd-zt-077/` | G3 - Protected-service vertical slice | BDD / integration | pending | godog | `features/go/authorization.feature` |
| ZT-78 | BDD-ZT-078 | Visualization UI | The UI presents core Zero Trust values such as connection status, mocked TEE measurements, sent and blocked messages. | GIVEN the client-approved visualization concept and a running FACIS.ZTD demonstrator WHEN the visualization UI is exercised through the ORCE-orchestrated user journey THEN the UI presents the core Zero Trust values, namely connection status, mocked TEE measurement values, and sent and blocked messages AND a blocked message is shown with its reason. | `evidence/bdd-zt-078/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-79 | BDD-ZT-079 | Visualization Concept | The visualization concept is documented in GitHub and aligned with the client. | GIVEN the initial demonstrator flow WHEN the wireframe/visualization concept is reviewed with FACIS THEN the concept is documented in GitHub and the client approval record is captured before UI implementation proceeds. | `evidence/shared/bdd-zt-042-079/` | G2 - Platform and documentation baseline (with BDD submission) | Documentation / client review | pending | Cucumber.js | `features/js/documentation.feature` |
| ZT-80 | BDD-ZT-080 | Zero Trust Configuration | The Zero Trust setup can be configured, including protected resources, participant backends, mock replacement, attestation procedures and DNS records. | GIVEN the approved demonstrator configuration interface WHEN an authorized operator changes supported settings such as protected resources, participant backends, mock replacement, attestation procedure or DNS/scenario configuration THEN the selected configuration is applied and reflected by the demonstrator AND invalid configuration is rejected with a machine-readable error. | `evidence/bdd-zt-080/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| ZT-81 | BDD-ZT-081 | User Journey Scenario | The visualization demonstrates a user journey orchestrated via ORCE; the scenario is aligned with the client and explained step by step in documentation and visualization. | GIVEN the client-approved user journey WHEN it is executed in ORCE THEN the journey is shown step by step, including credential issuance/presentation, trust decisions, protected-resource access and visual outcomes. | `evidence/shared/bdd-zt-046-081/` | G5 - Code ready for QA + ORCE user journey and visualization | UX/BDD | pending | Cucumber.js | `features/js/visualization.feature` |
| TDR-BDD-01 | BDD-TDR-001 | Successful deployment | A release can be deployed on the prescribed Kubernetes target environment through Helm with zero manual intervention. | GIVEN an approved release and target cluster WHEN the Helm/ORCE deployment workflow is executed THEN all required resources are created and the release reaches Ready state without manual intervention. | `evidence/bdd-tdr-001/` | G6 - Quality readiness | Deployment BDD (every release) | implemented | Cucumber.js | `features/js/lifecycle.feature` |
| TDR-BDD-02 | BDD-TDR-002 | Invalid deployment parameters | Invalid or incomplete deployment parameters are rejected with a machine-readable error and no partial trusted deployment remains. | GIVEN invalid or incomplete deployment parameters WHEN the deployment workflow is executed THEN deployment fails cleanly, returns a machine-readable error via the automation context, and no partial trusted state is left behind. | `evidence/bdd-tdr-002/` | G6 - Quality readiness | Negative deployment BDD (every release) | implemented | Cucumber.js | `features/js/lifecycle.feature` |
| TDR-BDD-03 | BDD-TDR-003 | Idempotent redeployment | Redeploying an unchanged release succeeds without manual intervention and without creating inconsistent duplicate resources. | GIVEN a successfully deployed release WHEN the same release is deployed again THEN the operation completes successfully and the resulting Kubernetes state remains consistent and ready. | `evidence/bdd-tdr-003/` | G6 - Quality readiness | Lifecycle BDD (every release) | implemented | Cucumber.js | `features/js/lifecycle.feature` |
| TDR-BDD-04 | BDD-TDR-004 | Uninstall | The release can be uninstalled through the documented workflow with zero manual intervention. | GIVEN a deployed release WHEN the uninstall workflow is executed THEN the release is removed without manual intervention and the expected project resources are no longer present. | `evidence/bdd-tdr-004/` | G6 - Quality readiness | Lifecycle BDD (every release) | implemented | Cucumber.js | `features/js/lifecycle.feature` |
| TDR-BDD-05 | BDD-TDR-005 | Keycloak / policy / token integration | Required Keycloak, policy and token integrations are reachable and produce the expected allow/deny behaviour. | GIVEN the target Keycloak, policy and token services are reachable WHEN a valid and an invalid access flow are executed THEN the valid flow is authorized and the invalid flow is denied with traceable evidence. | `evidence/bdd-tdr-005/` | G3 - Protected-service vertical slice | Integration BDD | pending | godog | `features/go/authorization.feature` |
| TDR-BDD-06 | BDD-TDR-006 | Structured logging and ORCE errors | Relevant runtime logs are structured JSON and automation errors are machine-readable and exposed through ORCE context. | GIVEN a controlled error condition WHEN it is triggered THEN the corresponding log is structured JSON and the machine-readable error is available through the ORCE context. | `evidence/bdd-tdr-006/` | G5 - Code ready for QA + ORCE user journey and visualization | Inspection / negative-path BDD | implemented | Cucumber.js | `features/js/orce-qa.feature` |
| TDR-BDD-07 | BDD-TDR-007 | TLS 1.3 runtime validation | TLS 1.3 is enforced on all relevant endpoints and older TLS protocol versions are rejected. | GIVEN the deployed endpoints WHEN TLS 1.3 and an older TLS version are attempted THEN TLS 1.3 succeeds where authorized and the older protocol version is rejected. | `evidence/bdd-tdr-007/` | G4 - Bidirectional inter-cluster trust | Security BDD | pending | godog | `features/go/security-baseline.feature` |
| TDR-BDD-08 | BDD-TDR-008 | Secrets and log masking | Credentials/secrets are stored in Kubernetes Secrets and no plaintext secret is exposed in application or pipeline logs. | GIVEN the deployed release and CI/CD logs WHEN secret storage and log output are inspected THEN required credentials are held in Kubernetes Secrets and no plaintext secret value is present in logs. | `evidence/bdd-tdr-008/` | G2 - Platform and documentation baseline (with BDD submission) | Security inspection | pending | godog | `features/go/security-baseline.feature` |
| TDR-BDD-09 | BDD-TDR-009 | Required Kubernetes objects | Namespace, ingress, service and deployment objects required for the release are created correctly. | GIVEN a successful release deployment WHEN the Kubernetes resources are inspected THEN the required namespace, ingress, service and deployment objects exist and are ready. | `evidence/bdd-tdr-009/` | G6 - Quality readiness | Deployment inspection (every release) | pending | godog | `features/go/release-quality.feature` |
| TDR-BDD-10 | BDD-TDR-010 | External dependency reachability | Required external dependencies, including Keycloak and trust endpoints, are reachable from the prescribed target environment. | GIVEN the target deployment WHEN dependency health/connectivity checks are executed THEN the required Keycloak and trust endpoints are reachable and their expected interfaces respond. | `evidence/bdd-tdr-010/` | G2 - Platform and documentation baseline (with BDD submission) | Integration readiness test | pending | godog | `features/go/environments.feature` |
| TDR-BDD-11 | BDD-TDR-011 | Helm lint and dry-run | Every release Helm chart passes lint and dry-run checks before promotion. | GIVEN a release candidate WHEN its Helm chart enters the CI quality gate THEN helm lint and helm dry-run both pass before the release can be promoted. | `evidence/bdd-tdr-011/` | G6 - Quality readiness | CI quality gate (every release) | pending | godog | `features/go/release-quality.feature` |
| M7-01 | BDD-M7-001 | Final Validation and Demonstration | The complete demonstrator is validated with the client in the agreed target environment. The approved user journey, the positive path and the refusal paths are demonstrated end to end, every demonstrated state is traceable to the per-gate evidence bundle, and no open Critical/High release blocker exists at the time of the demonstration. | GIVEN the release accepted at G6 and the agreed target environment WHEN the final validation session is executed with FACIS THEN the complete approved user journey, the positive path and the refusal paths are demonstrated end to end AND every demonstrated state is traceable to the per-gate evidence bundle AND no open Critical/High release blocker is present. | `evidence/bdd-m7-001/` | G7 - Final validation | Acceptance demonstration / client review | pending | Cucumber.js | `features/js/final-acceptance.feature` |
| M7-02 | BDD-M7-002 | Joint Final Report | The joint final report is prepared together with the client and contains resolvable references to the SRS, the Technical Development Requirements, the approved project plan, the agreed acceptance criteria and the per-gate evidence bundle. | GIVEN the completed per-gate evidence bundles and the accepted release WHEN the joint final report is prepared and reviewed with FACIS THEN the report is complete and every one of its five required references resolves to the referenced artefact AND the client records its acceptance of the report. | `evidence/bdd-m7-002/` | G7 - Final validation | Documentation / client review | pending | Cucumber.js | `features/js/final-acceptance.feature` |
