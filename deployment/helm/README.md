# Helm charts

Charts for the demonstrator. The umbrella chart installs a complete zone: management and data
planes in separate namespaces, baseline deny network policies, and installation ordered so workload
identity exists before any workload starts.

Each chart documents its values alongside it.
